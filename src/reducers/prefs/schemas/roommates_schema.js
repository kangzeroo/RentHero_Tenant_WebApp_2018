

export const ROOMMATES = {

    KEY: 'ROOMMATES',
    TENANT_ID: '',

    AGE: 18,
    GENDER: '',

    ALLERGIES_MEDICAL: '',
    WEEKDAY_SLEEPTIME: '',
    WEEKEND_SLEEPTIME: '',
    MOST_TIME_SPENT: '',
    DO_FOR_FUN: '',
    BRIEF_BIO: '',
    HATE_WHEN: '',

    MAX_ROOMMATES: 0,
    MIN_ROOMMATE_AGE: 18,
    MAX_ROOMMATE_AGE: 30,

    // acceptable gender of roommates
    // ACCEPTABLE_GENDER_AS: ['any gender'],                                              // save as CSV string
    // ACCEPTABLE_GENDER_AS_SCHEMAS: [{ id, text, value }],                              // save as stringified json
    ACCEPTABLE_GENDER_AS: [],                                              // save as CSV string
    ACCEPTABLE_GENDER_AS_SCHEMAS: [],                              // save as stringified json

    // lgbt identification
    // LGBT_AS: ['yes'],                                                     // save as CSV string
    // LGBT_AS_SCHEMAS: [{ id, text, value }],                              // save as stringified json
    LGBT_AS: [],                                                     // save as CSV string
    LGBT_AS_SCHEMAS: [],                              // save as stringified json

    // lgbt friendly
    // LGBT_FRIENDLY_AS: ['yes'],                                                     // save as CSV string
    // LGBT_FRIENDLY_AS_SCHEMAS: [{ id, text, value }],                              // save as stringified json
    LGBT_FRIENDLY_AS: [],                                                     // save as CSV string
    LGBT_FRIENDLY_AS_SCHEMAS: [],                              // save as stringified json

    // nearterm future goals
    // FUTURE_GOALS_AS: ['career', 'love'],                                          // save as CSV string
    // FUTURE_GOALS_AS_SCHEMAS: [{ id, text, value }],                              // save as stringified json
    FUTURE_GOALS_AS: [],                                          // save as CSV string
    FUTURE_GOALS_AS_SCHEMAS: [],                              // save as stringified json

    // drug use
    // DRUG_USE_AS: ['alcohol', 'cannabis'],                                     // save as CSV string
    // DRUG_USE_AS_SCHEMAS: [{ id, text, value }],                              // save as stringified json
    DRUG_USE_AS: [],                                     // save as CSV string
    DRUG_USE_AS_SCHEMAS: [],                              // save as stringified json

    // religion
    // RELIGION_AS: ['muslim'],                                                  // save as CSV string
    // RELIGION_AS_SCHEMAS: [{ id, text, value }],                              // save as stringified json
    RELIGION_AS: [],                                                  // save as CSV string
    RELIGION_AS_SCHEMAS: [],                              // save as stringified json

    // ethnicity
    // ETHNICITY_AS: ['east asian', 'white'],                                     // save as CSV string
    // ETHNICITY_AS_SCHEMAS: [{ id, text, value }],                              // save as stringified json
    ETHNICITY_AS: [],                                     // save as CSV string
    ETHNICITY_AS_SCHEMAS: [],                              // save as stringified json
}
