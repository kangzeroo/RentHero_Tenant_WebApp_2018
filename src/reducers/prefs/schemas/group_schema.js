


export const GROUP = {

      KEY: 'GROUP',
      TENANT_ID: '',

      GROUP_ID: '',
      GROUP_NAME: '',
      CERTAIN_MEMBERS: 1,
      UNCERTAIN_MEMBERS: 0,
      BATHROOMS: 0,
      BIO: '',

      // acceptable room types
      // ROOM_TYPES_AS: ['2 bed', '2+den', 'solo'],                                        // save as CSV string
      // ROOM_TYPES_AS_SCHEMAS: [{ id, text, value }]                                     // saved as stringified JSON
      ROOM_TYPES_AS: '',                                        // save as CSV string
      ROOM_TYPES_AS_SCHEMAS: [],                                     // saved as stringified JSON

      // group description
      // SEARCHING_AS: ['2 friends'],                                                      // save as CSV string
      // SEARCHING_AS_SCHEMAS: [{ id, text, value }]                                      // saved as stringified JSON
      SEARCHING_AS: '',                                                      // save as CSV string
      SEARCHING_AS_SCHEMAS: [],                                      // saved as stringified JSON

      // prefer whole place or ok with random roommates
      // WHOLE_OR_RANDOM_AS: ['whole place', 'random roommates'],                          // save as CSV string
      // WHOLE_OR_RANDOMS_AS_SCHEMAS: [{ id, text, value }],                              // saved as stringified JSON
      WHOLE_OR_RANDOM_AS: '',                          // save as CSV string
      WHOLE_OR_RANDOMS_AS_SCHEMAS: [],                              // saved as stringified JSON

      // will someone live in the den?
      // LIVE_IN_DEN_AS: ['show den'],                                                     // save as CSV string
      // LIVE_IN_DEN_AS_SCHEMAS: [{ id, text, value }],                                   // saved as stringified JSON
      LIVE_IN_DEN_AS: '',                                                     // save as CSV string
      LIVE_IN_DEN_AS_SCHEMAS: [],                                   // saved as stringified JSON

      // whats the family like?
      // FAMILY_MEMBERS_AS: ['2 adult', '1 elderly', '1 child'],                           // save as CSV string
      // FAMILY_MEMBERS_AS_SCHEMAS: [{ id, text, value }],                                // saved as stringified JSON
      FAMILY_MEMBERS_AS: '',                           // save as CSV string
      FAMILY_MEMBERS_AS_SCHEMAS: [],                                // saved as stringified JSON

      // any pets?
      // PETS_AS: ['2 large dogs', '1 bird'],                                              // save as CSV string
      // PETS_AS_SCHEMAS: [{ id, text, value }],                                          // saved as stringified JSON
      PETS_AS: '',                                              // save as CSV string
      PETS_AS_SCHEMAS: [],                                          // saved as stringified JSON

}
